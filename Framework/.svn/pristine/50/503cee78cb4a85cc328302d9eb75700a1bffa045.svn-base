package com.markit.common.framework.util;

import java.util.Map;

/**
 * This Util is to load XPaths 
 * present in XPath.properties
 *
 */
public class XPath {  
	public static Map<String, String> myprop;

	public static void init(String filePath){
		myprop=Config.loadConfig(filePath);
	}

}






































/*File f =new File(path);
InputStream is = null;
try {
	is= new FileInputStream(f);
	Properties p=new Properties();
	p.load(is);

	HashMap<String, String> propMap = new HashMap<String, String>((Map) p);

	myprop=propMap;


} catch (FileNotFoundException e) {		
	e.printStackTrace();
} catch (IOException e) {		
	e.printStackTrace();
}
*/